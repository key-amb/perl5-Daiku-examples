task 'default' => 'run';

task 'install' => sub {
    sh 'cpanm --installdeps .';
};

task 'run' => ['install', 'test'] => sub {
    sh 'perl -Ilib script/foo.pl';
};

task 'test' => 'install' => sub {
    sh 'prove -Ilib -rv t/';
};
